# Dependencies
node_modules/

# Environment variables (never commit real credentials)
.env
.env.local
.env.*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS files
.DS_Store
Thumbs.db

# Local / validation scripts and caches (e.g. one-off harnesses, temp output)
_*
node_modules/.cache/
*.log
