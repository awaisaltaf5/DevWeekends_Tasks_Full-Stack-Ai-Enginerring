# Server configuration
PORT=5000

# MongoDB connection string (use your own)
MONGODB_URI=your_mongodb_connection_string

# JWT secret - use a strong random string (never commit real secrets)
JWT_SECRET=your_jwt_secret

# JWT expiration time
JWT_EXPIRES_IN=7d